# JS Event Loop

- A three.js site showing a 3d representation of the JS event loop
- There are buttons to trigger a sim of the event loop in action so user can see what's usually going on in a normal application
- More specific categories, like async/await, micro/macro tasks, libuv, etc. where user can click and see that one individual tech and concept in action. With explanation of course.
